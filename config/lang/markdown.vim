if !exists('g:coc_global_extensions')
  let g:coc_global_extensions = []
endif

let g:coc_global_extensions += ['coc-markdownlint', 'coc-markdown-preview-enhanced']

" Otherwise tab width will be set to 4
let g:markdown_recommended_style = 0
